import React from "react";
import StartComponent from "./StartComponent";

function UserNotFoundComponent({ icon, text }) {
  return <StartComponent icon={icon} text={text} />;
}

export default UserNotFoundComponent;
