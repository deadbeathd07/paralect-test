import React from "react";
import StartComponent from "./StartComponent";

function ReposisEmpty({ icon, text }) {
  return <StartComponent icon={icon} text={text} />;
}

export default ReposisEmpty;
